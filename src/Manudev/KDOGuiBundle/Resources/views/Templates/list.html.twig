<section id="content">

    <section id="giftListBaner" class="tp-banner section-intro">
        <div class="stellar bg-overlay" data-stellar-background-ratio="0.4"
             style="background: url('http://k.loc/{{'{{ listCtrl.lists.picture.web_path}}'}}');
                     background-size: cover;
                     background-repeat: no-repeat;">
            <div class=" tp-dottedoverlay twoxtwo" ></div>
        </div>

        <div id="header-list-title" class="bg-overlay bg8 col-md-5 col-sm-8 mt20 mb20" style="z-index: 3">
            <h1 class="intro-title">{{ '{{ listCtrl.lists.title }}' }}</h1>
            <h3 style="color: white">{{ '{{ listCtrl.lists.sub_title }}' }}</h3>
            <p class="intro-p">{{ '{{ listCtrl.lists.description }}' }}</p>
            <div class="addthis_sharing_toolbox"></div>
        </div>
    </section>

    <section class="section-page">
        <div class="container">
            <div class="masonry">
                <gift-panel class="item" gift="gift" ng-repeat="gift in listCtrl.lists.gifts"></gift-panel>
            </div>
        </div>
    </section>

    {{ '{{ $scope.currentGift.name }}' }}
    <div class="modal fade" id="modal" tabindex="-1" role="modal" aria-labelledby="modal-label" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                    <h4 class="modal-title" id="modal-label">{{ '{{ $scope.currentGift.name }}' }}</h4>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-sm-12">
                            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Laudantium atque architecto quae ullam rem labore veritatis temporibus asperiores, ad eaque odit commodi inventore dolore perspiciatis, nam dicta est eos id quod excepturi.</p>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-b" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-b">Save Changes</button>
                </div>
            </div>
        </div>
    </div>
</section>